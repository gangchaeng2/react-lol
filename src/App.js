import React, { Component } from 'react';
import {SearchContainer} from './containers';

class App extends Component {
  render() {
    return (
        <div>
            <SearchContainer/>
        </div>
    );
  }
}

export default App;
